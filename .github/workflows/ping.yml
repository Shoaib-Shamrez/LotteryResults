name: Keep Backend Awake

on:
  schedule:
    - cron: "*/10 * * * *" # every 10 minutes
  workflow_dispatch: {}

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Vercel backend
        run: curl -fsS https://lottery-results-sigma.vercel.app/ping || true
